  <div class="page">
    <%= render 'header' %>
    <%= render 'homeSide' %>
      <section class="news">
	<h1 class="dataBanner"><a id="newsLink">News</a></h1>
	<div class="dataContent">
          <%= render partial: 'home_page_news', collection: @store_news, as: "store_news" %>
        </div>
      </section>
      <section class="deals">
	<h1 class="dataBanner"><a id="dealsLink">Deals</a></h1>
	<div class="dataContent">
           <%= render partial: 'home_page_deals', collection: @product_deals, as: "product" %>
        </div>
      </section>
      <section class="newItems">
        <h1 class="dataBanner"><a id="itemLink">New Items</a></h1>
	<div class="dataContent">
          <%= render partial: 'home_page_deals', collection: @product_new, as: "product" %>
        </div>
      </section>
      <footer class="footer">
        <%= render 'footer' %>
      </footer>
    </div>
