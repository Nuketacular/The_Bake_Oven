<div class="dataBox">
  <%= image_tag(giftware_products.photo, class: "dataImage") %>
    <div class="dataBoxContent">
      <h4><%= link_to giftware_products.name, show_products_path(giftware_products) %></h4>
      <p><%= giftware_products.desc %></p>
      <p><%= number_to_currency(giftware_products.price) %></p>
    </div>
</div>