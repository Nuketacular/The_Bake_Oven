<div class="dataBox">
  <%= image_tag(all_products.photo, class: "dataImage") %>
    <div class="dataBoxContent">
      <h4><%= all_products.name %></h4>
      <p><%= all_products.desc %></p>
      <p><%= number_to_currency(all_products.price) %></p>
    </div>
</div>